package demonstration

/**
 * Performs an addition with [x] and [y] integers (`x + y`).
 *
 * SAMPLE: [demonstration.IntSample.addition]
 */
public fun addition(x: Int, y: Int): Int = x + y

/**
 * Performs an addition with [x] and [y] integers (`x + y`).
 *
 * SAMPLE: [demonstration.LongSample.addition]
 */
public fun addition(x: Long, y: Long): Long = x + y

/**
 * Performs a subtraction with [x] and [y] integers (`x - y`).
 *
 * SAMPLE: [demonstration.IntSample.subtraction]
 */
public fun subtraction(x: Int, y: Int): Int = x - y

/**
 * Performs a subtraction with [x] and [y] integers (`x - y`).
 *
 * SAMPLE: [demonstration.LongSample.subtraction]
 */
public fun subtraction(x: Long, y: Long): Long = x - y
