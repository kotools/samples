# `conventions` included build

This build provides Gradle conventions plugins for Kotools Samples.

For discovering the Gradle tasks available from this project, run the following
command from the root project:

```shell
./gradlew :conventions:tasks
```
