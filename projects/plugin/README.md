# `samples-gradle-plugin` project

This project contains the source code of the Kotools Samples Gradle plugin.

For discovering the Gradle tasks available from this project, run the following
command from the root project:

```shell
./gradlew :samples-gradle-plugin:tasks
```
